

const BASE = '/examenes/api/';

const ordenes = BASE.concat('visiometria/recepcion/');
const visiometria = BASE.concat('visiometria/');
const visiometra = BASE.concat('visiometra/');
const ordenes_audiometria = BASE.concat('audiometria/recepcion/');
const audiometria = BASE.concat('audiometria/');

export default {
    ordenes,
    visiometria,
    visiometra,
    ordenes_audiometria,
    audiometria,
}
